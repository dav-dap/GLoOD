# --- Slurm directive defaults -------------------------------------
slurm:
  job_name: a_prof
  partition: nodes
  time: "02:00:00"
  nodes: 1
  ntasks: 1
  cpus_per_task: 1

# --- Launcher-specific defaults -----------------------------------
cmd: python3 -m glood.assess_profiles
prelude: []
sandbox: true
sandbox_root: git_ignore/runs/assess_profiles_remote_matnum

local: true

# --- Program-argument defaults ------------------------------------
program: {}
